package com.example.inventario.security

/** Opciones del menú interno de bodega (filtradas por rol). */
enum class BodegaMenuKey {
    INVENTARIO,
    ENTRADAS,
    SALIDAS,
    VALES,
    EXISTENCIAS,
    KARDEX,
    CATEGORIAS,
    FACTURAS,
    MOVIMIENTOS,
    ANALISIS,
    AUDITORIA_BODEGA
}
